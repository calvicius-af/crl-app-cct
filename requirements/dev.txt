# Constraints de desenvolvimento e testes: versões exactas usadas pelo CI.
#
# Acompanha requirements/runtime.txt e cobre o que só é preciso para
# desenvolver, testar e auditar:
#
#     python -m pip install -r requirements.txt \
#         -c requirements/runtime.txt -c requirements/dev.txt
#
# pip-audit não entra em requirements.txt de propósito: é ferramenta de CI,
# não dependência do produto.
pytest==9.1.1
pip-audit==2.10.1
