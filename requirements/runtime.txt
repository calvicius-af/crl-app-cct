# Constraints de runtime: versões exactas usadas pelo CI e por uma release.
#
# Porquê: requirements.txt declara mínimos (">=") para que a instalação local
# continue leve e tolerante; este ficheiro fixa o que foi efectivamente testado,
# para que uma corrida do CI e uma instalação de release sejam reproduzíveis.
# Usa-se como constraints, não como lista de instalação:
#
#     python -m pip install -r requirements.txt -c requirements/runtime.txt
#
# Actualização: ver CONTRIBUTING.md, secção "Actualizar versões".
pdfplumber==0.11.10
openpyxl==3.1.5
PyYAML==6.0.3
jsonschema==4.26.0
