# Dependências do pipeline CCT (instalar com: python -m pip install -r requirements.txt)
pdfplumber>=0.11
openpyxl>=3.1
pyyaml>=6.0
jsonschema>=4.0
# extrator docling, opcional (--extrator docling): pesado (PyTorch, ~4 GB)
# e requer Python arm64 no macOS Apple Silicon
# docling>=2.121
# desenvolvimento/testes (opcional em produção)
pytest>=8.0
