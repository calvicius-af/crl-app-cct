# Dependências do pipeline CCT (instalar com: python -m pip install -r requirements.txt)
pdfplumber>=0.11
openpyxl>=3.1
pyyaml>=6.0
jsonschema>=4.0
# extrator docling, opcional (--extrator docling): pesado (PyTorch, ~4 GB)
# e requer Python arm64 no macOS Apple Silicon
# docling>=2.121
# NÃO instalar docling-hierarchical-pdf: foi avaliado e retirado do código
# (rebentava com page_range, falhava em silêncio e a ordem de leitura passou
# a vir da geometria — ver cct/extractor_docling.py, ordenar_por_leitura).
# Para correr tests/test_docling_integracao.py basta docling-core, que é
# leve (~30 pacotes, sem PyTorch): python -m pip install docling-core
# desenvolvimento/testes (opcional em produção)
pytest>=8.0
